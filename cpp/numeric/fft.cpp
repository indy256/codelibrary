#include <bits/stdc++.h>
#include "fft.h"

using namespace std;

// usage example
int main() {
    vector<int> a{5, 1};
    vector<int> b{2, 1};
    vector<int> mul = a * b;
    for (int x:mul) cout << x << " ";
    cout << endl;
}
